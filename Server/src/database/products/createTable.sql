CREATE TABLE PRODUCTS (ID INT NOT NULL IDENTITY(1,1), TITLE VARCHAR(300) NOT NULL, PRICE INT NOT NULL, DESCRIPTION VARCHAR(1000) NOT NULL,
CATEGORY VARCHAR(200) NOT NULL, IMAGE VARCHAR(1000) NOT NULL, QUANTITY INT NOT NULL, RATING INT NOT NULL)